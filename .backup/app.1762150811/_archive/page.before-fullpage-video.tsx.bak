import HeroVideo from "./_components/HeroVideo";

export default function Home() {
  return (
    <div className="relative min-h-[90vh] text-white">
      {/* 背景ヒーロー（動画または静止画ポスター） */}
      <HeroVideo />

      {/* ヘッダー */}
      <header className="relative z-10">
        <div className="container flex items-center justify-between py-5">
          <div className="text-xl font-semibold tracking-wide">Travoru</div>
          <nav className="space-x-4 text-sm">
            <a href="#features" className="hover:opacity-80">Features</a>
            <a href="#how-it-works" className="hover:opacity-80">How it works</a>
            <a href="/new" className="btn-ghost">Create plan</a>
          </nav>
        </div>
      </header>

      {/* ヒーロー テキスト＋CTA */}
      <section className="relative z-10 min-h-[80vh] flex items-center">
        <div className="container">
          <h1 className="text-4xl sm:text-6xl font-semibold leading-[1.15] max-w-[52ch] drop-shadow">
            AIが、動画の高揚感そのままに旅程を描く。<br /> 
          </h1>
          <p className="text-white/90 mt-5 max-w-[60ch]">
            日程と興味を選ぶだけ。時間最適のリアルなプランを自動生成。
            主要OTAへのリンク導線も完備。<br /> 
          </p>
          <div className="mt-8 flex flex-wrap gap-3">
            <a href="/new" className="btn">今すぐ旅程を作る（無料）</a>
            <a href="#features" className="btn-ghost">人気の目的地</a>
          </div>
        </div>
      </section>

      {/* 写真グリッド：人気の目的地 */}
      <section id="features" className="relative z-10
      bg-white text-black">
        <div className="container py-20">
          <h2 className="text-2xl sm:text-3xl font-semibold mb-6">人気の目的地</h2>
          <p className="text-neutral-600 -mt-4 mb-6 text-sm">写真をクリックしてイメージを膨らませましょう。<br /> </p>

          <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
            {[
              { title: "Tokyo", img: "/images/tokyo.jpg", desc: "グルメ・カルチャー・夜景" },
              { title: "Kyoto", img: "/images/kyoto.jpg", desc: "寺社と四季、静かな街歩き" },
              { title: "Osaka", img: "/images/osaka.jpg", desc: "粉もん・ミックスカルチャー" },
            ].map((d) => (
              <article
                key={d.title}
                className="group relative rounded-2xl overflow-hidden shadow-[var(--shadow-soft)] transition-transform duration-300 will-change-transform hover:scale-[1.01]"
              >
                <img src={d.img} alt={d.title} loading="lazy" className="w-full h-56 object-cover" />
                <div className="absolute inset-0 bg-gradient-to-t from-black/50 via-black/10 to-transparent" />
                <div className="absolute bottom-3 left-3 text-white drop-shadow">
                  <h3 className="text-xl font-semibold">{d.title}</h3>
                  <p className="text-sm text-white/90">{d.desc}</p>
                </div>
              </article>
            ))}
          </div>

          <div className="mt-8">
            <a href="/new" className="btn">自分の旅程を作る</a>
          </div>
        </div>
      </section>

      {/* How it works（写真モック） */}
      <section id="how" className="relative z-10
      bg-neutral-50 text-black">
        <div className="container py-20">
          <h2 className="text-2xl sm:text-3xl font-semibold mb-6">How it works</h2>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            {/* Step 1 */}
            <div className="card p-0 overflow-hidden">
              <div className="p-5">
                <h3 className="font-semibold">Step 1 — 入力</h3>
                <p className="text-sm text-neutral-600">日程・都市・興味を選ぶだけ</p>
              </div>
              <div className="bg-white">
                <img src="/images/tokyo.jpg" alt="Form mock" className="w-full h-auto" loading="lazy" />
              </div>
            </div>

            {/* Step 2 */}
            <div className="card p-0 overflow-hidden">
              <div className="p-5">
                <h3 className="font-semibold">Step 2 — 生成</h3>
                <p className="text-sm text-neutral-600">移動時間や混雑も考慮した時間割を作成</p>
              </div>
              <div className="bg-white">
                <img src="/images/kyoto.jpg" alt="Itinerary mock" className="w-full h-auto" loading="lazy" />
              </div>
            </div>
          </div>

          <div className="card mt-6">
            <h3 className="font-semibold mb-2">Step 3 — 予約導線</h3>
            <p className="text-sm text-neutral-600 mb-4">主要OTAのリンクでスムーズに予約。<br /> </p>
            <div className="flex flex-wrap gap-2">
              <a className="btn" href="/new">今すぐ試す</a>
              <a className="btn-ghost" href="#features">人気の目的地を見る</a>
            </div>
          </div>
        </div>
      </section>

      {/* フッター直前の強いCTA */}
      <section className="relative z-10
      bg-white text-center py-16">
        <div className="container">
          <h3 className="text-2xl font-semibold">今すぐ旅程を作ってみる</h3>
          <p className="text-neutral-600 mt-2">無料プレビュー（回数制限あり）。詳細は購入後に解禁。<br /> </p>
          <div className="mt-6">
            <a href="/new" className="btn">Create plan</a>
          </div>
        </div>
      </section>

      {/* フッター */}
      <footer className="relative z-10
      bg-black text-white">
        <div className="container py-10">
          <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
            <div className="font-semibold tracking-wide">Travoru</div>
            <nav className="text-sm space-x-4 text-white/80">
              <a href="/new" className="hover:text-white">Create plan</a>
              <a href="#features" className="hover:text-white">Features</a>
              <a href="#how-it-works" className="hover:text-white">How it works</a>
            </nav>
          </div>
          <p className="text-xs text-white/60 mt-4">© {new Date().getFullYear()} Travoru</p>
        </div>
      </footer>
    </div>
  );
}
